{
  "name": "@toolkit-dev/viteconfig",
  "version": "0.0.1",
  "description": "Vite configuration for the repo",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": "./dist/esm/create-vite-config.js"
  },
  "scripts": {
    "compile": "tsc --build",
    "clean:cache": "rm -rf ./dist",
    "clean:deps": "rm -rf ./node_modules",
    "format": "prettier --write .",
    "lint": "eslint src/ --ext .js,.jsx,.ts,.tsx",
    "lint:fix": "pnpm lint --fix"
  },
  "dependencies": {
    "vite": "6.2.0",
    "vitest": "3.0.5"
  },
  "devDependencies": {
    "@toolkit-dev/tsconfig": "workspace:*",
    "@types/node": "22.13.14",
    "prettier": "3.3.3",
    "typescript": "5.7.3"
  }
}
