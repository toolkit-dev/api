/* -----------------------------------------------------------------------------
 * constants
 * -------------------------------------------------------------------------- */

export const API_PORT = 3000;
export const BASE_URL = `http://localhost:${API_PORT}`;
export const ALLOWED_ORIGINS = [/localhost:\d+/];
export const DEFAULT_ROUTE_DELAY = 1000;
