{
  "name": "@toolkit-dev/openapi-standard-schema",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "license": "MIT",
  "exports": {
    "./*": "./dist/esm/*.js"
  },
  "scripts": {
    "compile": "tsc --build",
    "clean:cache": "rm -rf ./dist",
    "clean:deps": "rm -rf ./node_modules",
    "format": "prettier --write .",
    "lint": "eslint src/ --ext .js,.jsx,.ts,.tsx",
    "lint:fix": "pnpm lint --fix",
    "test": "vitest run"
  },
  "dependencies": {
    "@standard-schema/spec": "1.0.0"
  },
  "devDependencies": {
    "@toolkit-dev/viteconfig": "workspace:*",
    "@types/node": "24.1.0",
    "typescript": "5.7.3",
    "vitest": "3.0.5"
  }
}
